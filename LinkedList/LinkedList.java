
public class LinkedList {
	
	//we do not know size of linked list so we cna't iterate directly to end.
 	
		 // Driver code 
	    public static void main(String[] args) 
	    { 
	    	
	    	Node head; // head of list
	    	int data;
	    	Node next; 
	    	Node new_node = new Node(data);
	    	new_node.next = null;
	    	
	    	public static NthToLast(LinkedList list, int data)  {
	    	
	    		Node( int d) {
	    			data = d;
	    			next = null; 
	    			
	    			for(int i = 0; i < n; i++) {
	    				head = head.next
	    			}
	    			
	    			while(head.next != null) {
	    				head = head.next;
	    				new_node = new_node.next;
	    			}
	    			return new_node;
	    		}
	    		
	        /* Start with the empty list. */
	        LinkedList list = new LinkedList(); 
	   
	           
	        // Insert the values 
	        list = insert(list, 1); 
	        list = insert(list, 2); 
	        list = insert(list, 3); 
	        list = insert(list, 4); 
	        list = insert(list, 5); 
	        list = insert(list, 6); 
	        list = insert(list, 7); 
	        list = insert(list, 8); 
	   
	        // Print the LinkedList 
	        System.out.println(NthToLast(list));
	    } 
	}
	}
		
	
